#include "monty.h"
/**
 * f_stack – serves to print the top
 * @head: serves as stack head
 * @counter: serves as line_number
 * Return: no return
*/
void f_stack(stack_t **head, unsigned int counter)
{
(void)head;
(void)counter;
bus.lifi = 0;
}
